# TASK_002 背景透過と多言語対応の改修

## 依頼内容 / 目的
- コンテキストメニューやダイアログの背景が透明に見えるため、背景と同じ色にする。
- 背景画像調整の文言を多言語対応する。
- ヘッダー、タブ、ミニプレーヤーの背景をより半透明にする。

## 対応内容・設計方針
- テーマ設定で `dialogTheme` / `popupMenuTheme` / `bottomSheetTheme` を背景色ベースの不透明色へ統一。
- `themeBackground` のアルファ値影響を避けるため `withAlpha(255)` で不透明化した色を使用。
- 背景画像モード時の透過率を 0.80 から 0.65 へ調整（ヘッダー・下部タブ・ミニプレーヤー）。
- 背景画像調整画面の適用ボタン文言を `AppLocalizations` キーへ置換。

## 修正箇所のパスと行数
- `lib/ui/app_root.dart`
  - テーマ背景の不透明化、`dialogTheme`/`popupMenuTheme`/`bottomSheetTheme` 調整
  - `appBarTheme` の透過率を 0.65 に調整
- `lib/ui/main_screen.dart`
  - `BottomNavigationBar` の背景透過率を 0.65 に調整
- `lib/ui/mini_player.dart`
  - ミニプレーヤー背景透過率を 0.65 に調整
- `lib/widgets/background_widgets.dart`
  - 「適用」→ `commonApply`（多言語化）

## 変更による影響範囲
- 背景画像を表示している時の UI 見え方（ヘッダー、下部タブ、ミニプレーヤー）が以前より透過される。
- ダイアログ／コンテキストメニュー／ボトムシートが背景画像に透けにくくなる。

## テスト項目
- 背景画像 ON の状態で、ダイアログ背景が透けないこと。
- コンテキストメニュー背景が背景色で表示されること。
- ヘッダー／タブ／ミニプレーヤーが以前より半透明に見えること。
- 背景画像調整画面の適用ボタンが言語切替で追従すること。

## テスト結果
- 実施日: 2026-02-13
- 結果: 変更ファイルの静的エラーチェック OK（`get_errors` で対象4ファイル No errors）
- 備考: 実機見た目確認（透過感、可読性）はユーザー環境で最終確認推奨。
